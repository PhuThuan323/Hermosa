name: Hermosa Coffee Deployment

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: sakurado222
          key: ${{ secrets.DEPLOY_KEY }}
          port: 22
          script: |
            cd ./Hermosa/hermosa_backend
            git fetch origin
            git checkout main
            git pull
            npm install
            pm2 restart "nodejs server"
